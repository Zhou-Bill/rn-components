import { StyleSheet } from "react-native";

export default StyleSheet.create({
  toast: {
    // position: 'absolute',
    // left: '50%',
    zIndex: 99,
    padding: 12,
    backgroundColor: 'rgba(0,0,0, 0.7)',
  },
  content: {
    color: '#FFFFFF',
  }
})